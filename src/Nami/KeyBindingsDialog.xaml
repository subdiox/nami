<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Nami.KeyBindingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:l="using:Nami.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Nami"
    mc:Ignorable="d"
    Title="{l:Tr Text='Key bindings'}"
    PrimaryButtonText="{l:Tr Text='Save'}"
    CloseButtonText="{l:Tr Text='Close'}"
    DefaultButton="Primary"
    PrimaryButtonClick="OnSave">

    <ContentDialog.Resources>
        <DataTemplate x:Key="BindingTemplate" x:DataType="local:KeyBindingRow">
            <Grid ColumnSpacing="10" Padding="0,3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{x:Bind Key}" FontFamily="Cascadia Mono, Consolas" FontSize="12" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="{x:Bind Command}" FontFamily="Cascadia Mono, Consolas" FontSize="12" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="{x:Bind Source}" FontSize="11" Opacity="0.6" VerticalAlignment="Center" />
            </Grid>
        </DataTemplate>
    </ContentDialog.Resources>

    <Grid Width="640" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FilterBox" Grid.Column="0" PlaceholderText="{l:Tr Text='Filter by key or command'}" TextChanged="FilterBox_TextChanged" />
            <CheckBox x:Name="ShowDefaults" Grid.Column="1" Content="{l:Tr Text='Show mpv defaults'}" IsChecked="True" Click="FilterBox_TextChanged" />
        </Grid>

        <ListView x:Name="List" Grid.Row="1" ItemTemplate="{StaticResource BindingTemplate}" SelectionMode="Single"
                  MinHeight="200" MaxHeight="360" SelectionChanged="List_SelectionChanged" />

        <Grid Grid.Row="2" ColumnSpacing="8" RowSpacing="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBox x:Name="KeyBox" Grid.Row="0" Grid.Column="0" Header="{l:Tr Text='Key (press to capture)'}" PlaceholderText="{l:Tr Text='e.g. Ctrl+r'}" PreviewKeyDown="KeyBox_PreviewKeyDown" IsSpellCheckEnabled="False" />
            <TextBox x:Name="CommandBox" Grid.Row="0" Grid.Column="1" Header="{l:Tr Text='mpv command'}" PlaceholderText="{l:Tr Text='e.g. seek 10 ; show-text Forward'}" IsSpellCheckEnabled="False" />
            <Button x:Name="AddButton" Grid.Row="0" Grid.Column="2" Content="{l:Tr Text='Add / update'}" VerticalAlignment="Bottom" Click="Add_Click" />
            <Button x:Name="RemoveButton" Grid.Row="0" Grid.Column="3" Content="{l:Tr Text='Delete'}" VerticalAlignment="Bottom" Click="Remove_Click" IsEnabled="False" />
            <TextBlock Grid.Row="1" Grid.ColumnSpan="4" FontSize="11" Opacity="0.7" TextWrapping="Wrap">
                <Run Text="{l:Tr Text='Saving writes input.conf and applies the bindings immediately. See the mpv manual (Input Commands) for the command syntax.'}" />
            </TextBlock>
        </Grid>
    </Grid>
</ContentDialog>

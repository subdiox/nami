<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Nami.InspectorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:l="using:Nami.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Nami"
    mc:Ignorable="d"
    Title="{l:Tr Text='メディア情報'}"
    CloseButtonText="{l:Tr Text='閉じる'}"
    DefaultButton="Close">

    <ContentDialog.Resources>
        <DataTemplate x:Key="RowTemplate" x:DataType="local:InspectorRow">
            <Grid ColumnSpacing="12" Padding="0,3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{x:Bind Name}" FontSize="12" Opacity="0.7" VerticalAlignment="Top" />
                <TextBlock Grid.Column="1" Text="{x:Bind Value}" FontSize="12" TextWrapping="Wrap" IsTextSelectionEnabled="True" FontFamily="Cascadia Mono, Consolas" />
            </Grid>
        </DataTemplate>
    </ContentDialog.Resources>

    <ScrollViewer MaxHeight="600" Width="620">
        <ItemsControl x:Name="Rows" ItemTemplate="{StaticResource RowTemplate}" />
    </ScrollViewer>
</ContentDialog>
